# 数据标注与训练说明

## 1. 现在的系统是什么

现在只保留网页语音访谈版 `web_app.py`。系统由三部分组成：

1. CosyVoice：把AI的问题转换为语音。
2. SenseVoice：把参与者的语音转成初步文字。
3. 访谈程序：按题库提问、录音、展示转写结果，并根据规则决定是否追问。

CosyVoice和SenseVoice先直接使用现成模型，不需要拿患者数据重新训练。以后真正需要训练的是“回答后应该追问什么”这一部分。流畅度统计和t检验属于研究分析，不等于训练对话模型。

## 2. 数据怎么收集

一次完整的“AI提问 + 参与者回答”作为一个标注单位，不要把同一句回答任意切成很多独立样本。

每次访谈至少保存：

- 匿名参与者编号，不保存姓名、手机号、身份证号等直接身份信息。
- 场景：普通交流、就医交流或其他实验条件。
- 题目编号和AI实际说出的题目。
- 参与者原始回答音频。
- SenseVoice自动转写文本。
- 人工核对后的正确文本。
- AI追问内容和研究人员是否认可这次追问。
- 录音、转写、题库和程序版本。

同一个参与者的所有回答必须使用相同的匿名编号。原始音频、身份对应表和研究分析数据应分开保存并分别控制权限。

## 3. 一轮回答需要标什么

建议使用下面这些字段。第一阶段不必一次把所有字段做满，可以先标必填项。

| 字段 | 取值或例子 | 是否必填 | 含义 |
| --- | --- | --- | --- |
| participant_id | P0001 | 是 | 匿名参与者编号 |
| session_id | P0001_H01 | 是 | 一次访谈编号 |
| condition | baseline / healthcare | 是 | 普通交流或就医交流 |
| question_id | Q01 | 是 | 核心题目编号 |
| turn_id | 0001 | 是 | 本次问答轮次 |
| audio_file | audio/P0001_H01_0001.wav | 是 | 回答音频相对路径 |
| asr_text | 自动转写结果 | 是 | SenseVoice输出，不直接覆盖 |
| corrected_text | 人工核对文本 | 是 | 与音频一致的最终文本 |
| answer_relevance | 0、1、2 | 是 | 0偏题，1部分相关，2直接回答 |
| answer_completeness | 0、1、2 | 是 | 0基本未答，1不完整，2信息足够 |
| chronological_clarity | 0、1、2 | 建议 | 0顺序混乱，1部分清楚，2顺序清楚 |
| ambiguity | 0、1、2 | 建议 | 0明确，1需要少量澄清，2无法确定含义 |
| communication_difficulty | 0或1 | 是 | 参与者是否明确表示难以组织或表达 |
| distress_flag | 0或1 | 是 | 是否出现明显痛苦，需要减慢或暂停 |
| safety_flag | 0或1 | 是 | 是否出现需要人工接管的安全内容 |
| followup_needed | 0或1 | 是 | 是否应追问 |
| followup_type | 下表类别 | 是 | 应该采用哪一种追问 |
| approved_followup | 人工写出的追问 | 建议 | 可接受的标准追问 |
| annotator_id | A01 | 是 | 标注者编号 |
| notes | 简短说明 | 可选 | 只记录无法由字段表达的情况 |

### 追问类型

| 标签 | 什么时候用 | 例子 |
| --- | --- | --- |
| NONE | 回答已经足够，可以进入下一核心题 | 不追问 |
| CLARIFY | 指代、含义或事件不明确 | “你刚才说的‘那件事’具体指什么？” |
| TIMELINE | 时间或先后顺序不清楚 | “这件事发生在就诊前还是就诊后？” |
| DETAIL | 缺少理解事件所需的事实 | “当时具体发生了什么？” |
| IMPACT | 需要了解事件造成的影响 | “这件事对你后来的就医有什么影响？” |
| EMOTION | 需要了解当时的感受 | “当时你最明显的感受是什么？” |
| COMMUNICATION | 需要了解为什么难以表达 | “当时有什么让你觉得不容易说清楚？” |
| SUPPORT | 参与者明显痛苦，应先支持而非继续逼问 | “我们可以慢一点，或者先跳过这个问题。” |
| SAFETY | 出现自伤、他伤或紧急风险，由人工接管 | 程序停止自动追问 |

## 4. 标注规则

### 4.1 转写

- `asr_text` 永远保留机器原始结果。
- `corrected_text` 由标注者边听音频边修改。
- “嗯、呃、额”、重复、说到一半重新开始等内容不要删除，因为它们可能用于流畅度分析。
- 咳嗽、笑声和较长停顿可以写成 `[咳嗽]`、`[笑]`、`[停顿 1.2秒]`。
- 听不清时写 `[听不清]`，不要猜测。

### 4.2 回答质量

回答质量不能只按“说得顺不顺”判断。内容完整但有正常停顿，仍可以是高质量回答；说得很快但没有回答问题，也不能算高质量。

- 相关性：有没有回答当前问题。
- 完整性：是否提供了足以理解回答的信息。
- 时间清晰度：事件顺序能否理解。
- 模糊程度：关键人物、事件、原因或指代是否不清楚。
- 表达困难：参与者是否因压力、顾虑或组织困难而无法说清楚。

不要通过说话方式推断诊断、性别身份真实性或疾病严重程度。

### 4.3 流畅度

程序可以自动提取以下客观指标，但人工要抽样核对：

- 回答开始延迟：AI问题结束到参与者开始回答的时间。
- 发声内停顿比例：回答过程中停顿时间占发声段总时长的比例。
- 长停顿次数：可先把1秒及以上作为长停顿，再根据预实验调整。
- 语速：每分钟字数或音节数。
- 填充词率：“嗯、呃、额”等占总字数或总音节数的比例。
- 重复与自我修正率：重复、改口、句子重启的次数除以回答长度。

这些指标要在“普通交流”和“就医交流”两个条件下分别汇总到参与者层级。一个人切成20段仍然只是一个独立参与者，不能把20段当作20个人来计算t检验。

## 5. 标注质量控制

1. 先由两名标注者共同试标20至30轮，讨论并修改规则。
2. 正式数据中随机抽取至少20%至30%，由两人独立标注。
3. 两人看不到对方结果，分歧由第三人或研究负责人裁决。
4. 类别字段计算Cohen's kappa；0、1、2等级字段计算加权kappa或ICC。
5. 如果一致性较差，先修改定义并重新试标，不要直接开始训练。
6. 安全风险标签以召回为优先：宁可交给人工多检查一次，也不能让模型自动忽略。

## 6. 怎么训练

### 阶段一：先不训练

小样本阶段继续使用当前规则系统。收集AI建议、人工最终选择和修改后的追问，这些记录才会形成可训练的数据。此阶段的目标是证明访谈流程能运行，并把标注规范稳定下来。

### 阶段二：训练“是否追问和追问类型”

输入：当前题目、参与者本轮回答、最多前两轮对话。

输出：`NONE / CLARIFY / TIMELINE / DETAIL / IMPACT / EMOTION / COMMUNICATION / SUPPORT / SAFETY`。

第一版建议使用中文文本编码器加分类头，例如MacBERT或中文RoBERTa，而不是从零训练大语言模型。它本质上是Transformer分类模型。模型只决定追问类型，真正的问题仍从经过审核的模板中生成，风险更可控。

训练步骤：

1. 使用人工核对后的 `corrected_text`，不直接用未经修改的ASR文本作金标准。
2. 按 `participant_id` 划分训练集、验证集和测试集，例如70% / 15% / 15%。
3. 同一个参与者的全部录音只能出现在一个集合中，防止数据泄漏。
4. 类别不平衡时使用类别权重或分层采样，但仍按参与者分组。
5. 在训练集上更新模型，在验证集上选择阈值，测试集只在最后使用一次。
6. 保存模型权重、标签表、题库、分词器、阈值和训练配置，版本必须对应。

如果只有约30名参与者，不建议声称已经训练出可临床使用的模型。可以做原型，并使用按参与者分组的5折交叉验证；正式模型需要更多独立参与者和足够数量的少数类样本，尤其是SUPPORT和SAFETY。

### 阶段三：受控生成追问

数据量足够后，可以加入本地语言模型，但它只能：

- 在选定的追问类型内改写问题。
- 使用当前回答中已经出现的信息。
- 一次只问一个问题。
- 不诊断、不评价身份、不诱导回答。

生成结果先经过规则检查。涉及SAFETY时禁止语言模型自由生成，直接停止并交由人工处理。

## 7. 怎么评价训练结果

不要只看accuracy。至少报告：

- Macro-F1：每种追问类型是否都能识别。
- 各类别precision、recall和F1。
- SAFETY与SUPPORT的recall。
- 不必要追问率：原本应进入下一题却继续追问的比例。
- 漏追问率：回答明显不完整却没有追问的比例。
- 混淆矩阵：模型经常把哪些追问类型混在一起。
- 人工接受率：研究人员无需修改就接受模型追问的比例。

最终测试必须按参与者留出，不能把同一个人的不同回答随机分到训练集和测试集。

## 8. 与t检验的关系

对话模型训练和研究假设检验是两件事：

- 对话模型回答“下一步应该怎么问”。
- t检验回答“同一批参与者在就医交流中是否比普通交流更不流畅”。

如果同一个参与者同时完成普通交流和就医交流，应先对每个参与者、每个条件计算一个汇总值，再对两列数据做配对t检验。结果应报告均值差、95%置信区间、t值、自由度、p值和效应量。不能先用普通数据算一个t值，再只用真实数据算p值；t值和p值来自同一次比较。

## 9. 最小可执行版本

1. 固定核心题库和CosyVoice声音。
2. 用当前程序完成访谈并保留原始音频。
3. 人工核对SenseVoice转写。
4. 每轮先标相关性、完整性、是否追问、追问类型和安全标志。
5. 两名标注者复核部分数据。
6. 先用规则系统运行；数据积累后再训练追问分类器。
7. 流畅度指标按参与者汇总后单独做配对t检验。

这套顺序可以避免在没有真实标注数据时先训练一个看似复杂、但无法回答研究问题的模型。
